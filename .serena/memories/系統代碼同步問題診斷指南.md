# 系統代碼同步問題診斷指南

## 問題現象
測試環境正常，但生產環境出現不一致行為：
- 語法錯誤已修復，但 LLM 指導仍返回技術性描述
- 查詢如 "CNC車床今天不良率" 應觸發自然指導，但返回技術性回應

## 根本原因分析
1. **模組載入路徑問題**: 服務工廠可能載入舊版本的 AI 服務
2. **服務單例問題**: 舊版本服務可能已在記憶體中被快取
3. **方法呼叫路徑錯誤**: 系統可能仍使用舊版 `enhance_natural_language_query` 而非新版 `generate_user_guidance`

## 檢測步驟

### 1. 確認實際載入的服務類型
```python
# 檢查載入的 AI 服務類型
factory = get_enhanced_service_factory()
ai_service = factory.get_ai_model_service()
print(f"服務類型: {type(ai_service).__name__}")
print(f"有 generate_user_guidance: {hasattr(ai_service, 'generate_user_guidance')}")
```

### 2. 檢查方法呼叫路徑
```bash
# 搜尋所有對 AI 服務的呼叫
grep -r "enhance_natural_language_query" apps/bot/src/
grep -r "generate_user_guidance" apps/bot/src/
```

### 3. 確認 NL-to-SQL 服務的方法調用
```python
# 檢查 _generate_user_guidance 是否使用正確方法
# 在 nl_to_sql_service.py 第 428 行
ai_response = await self.ai_model_service.generate_user_guidance(...)
```

## 修復策略

### 立即修復 (已執行)
- ✅ 修復語法錯誤 (ai_model_service.py:531)
- ✅ 確認新方法存在

### 深度修復 (進行中)
1. **檢查服務註冊順序**: 確保 EnhancedAIModelService 正確註冊
2. **驗證方法路由**: 確認 nl_to_sql_service 使用正確的 AI 方法
3. **測試整合路徑**: 從 MessageHandlerDI → NL-to-SQL → AI 服務的完整路徑

### 預防措施
1. **開發環境對齊**: 確保開發和生產使用相同的服務載入路徑
2. **服務重啟**: 在修改核心服務後，必須完全重啟應用
3. **單元測試**: 新增針對 generate_user_guidance 的測試

## 問題模式
- **症狀**: 測試正常，生產異常
- **根因**: 服務單例快取 + 方法路由問題
- **修復**: 確認完整的方法呼叫鏈條

## 驗證方法
```bash
# 1. 檢查語法
python -m py_compile apps/bot/src/services/ai_model_service.py

# 2. 檢查服務載入
cd apps/bot && python -c "from src.infrastructure.enhanced_service_factory import get_enhanced_service_factory; print('OK')"

# 3. 測試整合
./quick-test-vocabulary.sh
```

## 成功標準
- "CNC車床今天不良率" 返回自然、友善的 LLM 指導
- 不再返回技術性描述如 "查詢CNC車床在今天的生產不良率，包含..."
- 所有空查詢都正確觸發 LLM 指導模式