# T-02 M001機台查詢驗證更新報告

## 📋 修復進展

**測試任務**: T-02 M001機台查詢驗證  
**執行時間**: 2025-06-26 09:42  
**修復狀態**: ✅ 部分成功（發現第二個問題）

## 🔧 第一個問題修復成功

### ✅ MCP 工具調用問題已解決
**問題**: `Unknown tool: read_query`  
**解決方案**: 修復 `database_service.py` 第56行
- **錯誤調用**: `"read_query"` + `{"query": sql_query}`
- **正確調用**: `"query"` + `{"sql": sql_query}`

**修復效果**:
```
✅ 工具調用成功：postgres.query
✅ 通信成功，發現 1 個工具
✅ MCP 連接成功：postgres
```

## 🔍 發現第二個問題

### ❌ 資料庫結構不匹配
**新錯誤**: `column m.machine_id does not exist`

### 問題分析

#### 成功案例 (test_m001_final.py)
```sql
SELECT 
    id as machine_id,
    name as machine_name,
    location as department,
    utilization_rate,
    status,
    temperature,
    last_maintenance,
    created_at
FROM machines 
WHERE id = 'M001'
```

#### 失敗案例 (ApplicationFacade)
```sql
SELECT m.machine_id, m.machine_name, m.department, 
       COALESCE(AVG(u.utilization_rate), 0) as avg_utilization_rate
FROM machines m 
LEFT JOIN utilization_records u ON m.machine_id = u.machine_id
WHERE m.machine_id = 'M001'
GROUP BY m.machine_id, m.machine_name, m.department
```

### 核心差異
1. **列名不匹配**:
   - 實際資料庫：`id`, `name`, `location`
   - SQL 模板使用：`machine_id`, `machine_name`, `department`

2. **查詢複雜度**:
   - 成功版本：簡單查詢單表
   - 失敗版本：複雜查詢多表 JOIN

## 📊 修復驗證結果

### ✅ 已修復項目
1. **MCP 工具調用**: 完全修復
2. **工具名稱**: 正確使用 `"query"`
3. **參數格式**: 正確使用 `{"sql": ...}`
4. **連接健康**: PostgreSQL MCP 連接穩定

### ❌ 仍需修復項目
1. **SQL 模板列名**: 需要與實際資料庫結構匹配
2. **模板複雜度**: 可能需要簡化查詢邏輯

## 🎯 對比測試結果

### test_m001_final.py (✅ 成功)
```
🔧 機台編號: M001
🏭 機台名稱: CNC車床A
📍 部門: 加工部
📊 稼動率: 74.40%
🟢 狀態: running
🌡️ 溫度: 45.50°C
```

### ApplicationFacade (❌ 失敗)
```
❌ MCP 查詢失敗 (unknown): column m.machine_id does not exist
```

## 🛠️ 下一步修復計劃

### 立即行動
1. **檢查 SQL 模板配置**:
   - 位置：`src/services/nl_to_sql/config/sql_templates.yaml`
   - 修復：更新 `specific_machine` 模板的列名

2. **使用成功的 SQL 結構**:
   ```sql
   SELECT 
       id as machine_id,
       name as machine_name,
       location as department,
       utilization_rate,
       status
   FROM machines 
   WHERE id = 'M001'
   ```

### 驗證步驟
1. 修復 SQL 模板
2. 重新測試 ApplicationFacade M001 查詢
3. 確認返回 74.40% 稼動率

## 📈 修復進度評估

**第一階段修復**: ✅ 完成 (MCP 工具調用)  
**第二階段修復**: 🔄 進行中 (SQL 模板結構)  
**最終目標**: 74.4% 稼動率查詢成功

## 📝 技術學習

### 成功經驗
1. **參考成功案例**: test_m001_final.py 提供了正確的實現方式
2. **分層修復**: 先修復工具調用，再修復業務邏輯
3. **詳細日誌**: 幫助快速定位問題根源

### 待改進項目
1. **SQL 模板維護**: 需要與實際資料庫結構保持同步
2. **測試一致性**: 確保所有測試使用相同的資料庫結構

---

**報告生成時間**: 2025-06-26 09:45  
**下次更新**: SQL 模板修復後