# AI 解析器調試指南

本指南提供了調試 AI 解析器對「CNC車床今天不良率」查詢處理過程的完整方法。

## 🎯 調試目標

測試 AI 解析器是否正確識別「不良率」等品質相關查詢為不支援的查詢類型，並返回適當的低信心度。

## 📁 調試腳本

### 1. 完整調試腳本
**文件**: `debug_ai_parser.py`

**功能**:
- 顯示完整的系統提示詞
- 檢查關鍵字是否存在於系統提示詞中
- 調用 AI 服務獲取原始回應
- 執行完整解析流程
- 進行結果驗證
- 進行對比測試（多個相關查詢）

**運行方式**:
```bash
cd apps/bot
python ../../debug_ai_parser.py
```

### 2. 簡化測試腳本
**文件**: `test-ai-parser-simple.py`

**功能**:
- 快速測試核心功能
- 顯示 AI 原始回應
- 驗證解析結果
- 適合快速診斷

**運行方式**:
```bash
cd apps/bot
python ../../test-ai-parser-simple.py
```

## 🔍 預期結果

### 正確的行為應該是：

1. **系統提示詞檢查**:
   - ✅ 包含「不良率」關鍵字
   - ✅ 包含「< 0.5」信心度指導
   - ✅ 包含「unknown」類型指導

2. **AI 原始回應**:
   - 應該返回 JSON 格式
   - `query_type` 應該是 "unknown"
   - `confidence` 應該 < 0.5
   - 應該包含品質指標相關的說明

3. **解析結果**:
   - 查詢類型: `QueryType.UNKNOWN`
   - 信心度: < 0.5
   - 解釋中應提及品質相關概念

### 示例預期輸出：

```json
{
  "query_type": "unknown",
  "entities": ["CNC車床", "不良率"],
  "parameters": {},
  "confidence": 0.3
}
```

## 🛠️ 問題診斷

### 如果 AI 解析器沒有正確識別：

1. **檢查系統提示詞**:
   - 確認包含品質指標的明確指導
   - 檢查範例是否包含「不良率」

2. **檢查 AI 服務配置**:
   - 確認使用的是正確的 AI 模型
   - 檢查 API 金鑰和配置

3. **檢查解析邏輯**:
   - 驗證 `_parse_ai_result` 方法
   - 檢查 `_create_query_from_ai_data` 方法

### 常見問題：

1. **模組導入失敗**:
   ```
   ❌ 模組導入失敗: No module named 'infrastructure'
   ```
   **解決方案**: 確保在 `apps/bot` 目錄下運行腳本

2. **AI 服務不可用**:
   ```
   ❌ 無法獲取 AI 模型服務，請檢查配置
   ```
   **解決方案**: 檢查 `.env` 文件中的 API 金鑰設置

3. **信心度過高**:
   ```
   ⚠️ 信心度過高 (>= 0.5): 0.8
   ```
   **解決方案**: 檢查系統提示詞是否正確指導 AI 返回低信心度

## 🔧 調試技巧

1. **逐步調試**:
   - 先運行簡化腳本確認基本功能
   - 再運行完整腳本進行詳細分析

2. **比較測試**:
   - 測試正常的查詢（如「M001稼動率」）
   - 測試其他品質相關查詢（如「合格率」）
   - 對比不同查詢的處理結果

3. **日誌分析**:
   - 注意 AI 解析器的日誌輸出
   - 檢查信心度計算過程
   - 查看解析邏輯的執行路徑

## 📝 自定義測試

您可以修改腳本來測試其他查詢：

```python
# 在腳本中修改測試查詢
test_queries = [
    "CNC車床今天不良率",
    "產品合格率統計",
    "品質檢驗結果",
    "M001今天良品率"
]

for query in test_queries:
    result = await parser.parse(query)
    print(f"{query} -> {result.query_type.value} (信心度: {result.confidence})")
```

## 🎉 驗證成功

當調試腳本顯示以下結果時，表示 AI 解析器工作正常：

- ✅ 正確識別為 UNKNOWN 查詢類型
- ✅ 信心度正確 (< 0.5)
- ✅ 解釋中正確提到品質相關概念
- ✅ 系統提示詞包含所有必要的指導內容

這表明 AI 解析器能夠正確識別並拒絕處理品質相關的查詢，符合系統設計的預期行為。